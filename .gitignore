node_modules/
.env
.env.local
.env.deploy
*.log
*.bak
.DS_Store
ca.crt
/var/
*.dump
.idea/
.vscode/

# Build output
dist/
*.tsbuildinfo

# Старые root-owned папки на сервере (после миграции с rsync на git pull)
*_old_root/

# Apple Pages document templates (binary, держим локально)
*.pages

# Большие исходники картинок в корне — оставляем локально (мастер-
# файлы для будущего экспорта в favicon/manifest), но в репо не тащим.
/Gemini_*.png
/*.heic
/*.psd
/*.ai
.claude/
